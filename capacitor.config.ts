import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.ndr.app.ndrhh',
  appName: 'messi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
